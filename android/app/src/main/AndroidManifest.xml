<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      The voice session calls getUserMedia({ audio: true }) directly, with no
      Capacitor plugin behind it, so nothing declares these for us.

      BOTH are required, and MODIFY_AUDIO_SETTINGS is the non-obvious one.
      Capacitor's BridgeWebChromeClient.onPermissionRequest asks for
      MODIFY_AUDIO_SETTINGS *and* RECORD_AUDIO together for an AUDIO_CAPTURE
      request, then grants the WebView only if every permission in that batch
      came back granted. MODIFY_AUDIO_SETTINGS is a normal permission, granted
      at install time — but only when the manifest declares it. Leave it out
      and Android reports it denied, the batch fails, and getUserMedia is
      refused *after* the user taps Allow on the microphone prompt.

      `required="false"` keeps the Play Store from hiding the app from devices
      without a microphone; the session already fails gracefully on those.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
